# Configurações de conexão com o banco de dados MySQL
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot_erudio?useSSL=false&serverTimezone=UTC
    username: root
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver

# Configurações do Hibernate
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: false